<!DOCTYPE html>
<html>
  <head>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.css">
    <title>TemplateDir</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= Gon::Base.render_data %>   
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.css">
    <link rel="stylesheet" href="application.css">
    <title>Document</title>
    <ul>
      <li class="menu_on_left"><a class="active" href="#home">Polyglog</a></li>
      <li class="menu_on_left"><%= link_to "People", '/people/index'%></li>
      <li class="menu_on_left"><%= link_to "message", '/messages/index'%></li>


      <% if current_person != nil %>
        <li class="menu_on_left"><%= link_to "Profile", "/people/#{current_person.id}" %></li>
      <% end %>
      <%if person_signed_in? %>
        <%if current_person.language_to_study != nil && current_person.native_language != nil%>
          <li class="menu_on_left"><%= link_to "new", "/messages/add/#{current_person.id}"%></li>
        <% end %>
        <li class="menu_on_right"><%= link_to "logout", destroy_person_session_path, method: :delete%></li>
      <%else%>
        <li class="menu_on_right"><%= link_to "login", new_person_session_path%></li>
      <%end%>
    </ul>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
